name: ludo_game

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flame: ^1.10.0
  flame_audio: ^2.0.0
  provider: ^6.0.0
  socket_io_client: ^2.0.0
  firebase_core: ^2.0.0
  cloud_firestore: ^4.0.0

flutter:
  assets:
    - assets/board/
    - assets/tokens/
    - assets/sounds/
